prog
    var
        i: int;
        somma: int;
        numeri: int[5];
    endvar

    somma <- 0;

    for i <- 0 to 4 do
        numeri[i] <- i + 1;
        somma <- somma + numeri[i];
    endfor;

    --> $(somma);
endprog
